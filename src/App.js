import router from './route';
import { RouterProvider } from 'react-router-dom';

// fichier qui contient toute l'application

function App() {
    return (
        < RouterProvider router = { router } />
    );
}

export default App;